interface:
  display_name: "Orchestrate Tickets"
  short_description: "Supervise Orca ticket DAGs and repair gates"
  default_prompt: "Use $orchestrate-tickets to implement the selected ready-for-agent tickets with supervised Pi workers and retain final review approval."

policy:
  allow_implicit_invocation: false
